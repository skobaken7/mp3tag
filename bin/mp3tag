#!/usr/bin/env ruby

require "bundler/setup"
require "mp3tag"

command = ARGV.shift

case command 
when "search"
  Mp3tag::Commands::Search.new(ARGV).exec
when "show"
  Mp3tag::Commands::Show.new(ARGV).exec
end

